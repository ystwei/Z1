1.final
  a.类：final 类不能被继承。
  b.方法：final方法不能被重写。
  c.变量：final变量不能被修改。
2.常量：大家都知道的 一出生就有的 不能改变的。
        public       static         final int OLD=100;
3.abstract：代表一个成员在本类中并不能实现，必须由其子孙实现的方法定义为抽象方法。
  有抽象方法的类一定是抽象类
  私有的不能放在抽象的成员前面
4.作业：计算不同图行的面积和周长
5.作业：接口interface重要特性：
  a。接口的方法都是抽象的。
  b。接口的方法的作用域修饰符是public的。
  c. 类和接口的关系是实现 implements,且可以多实现。解决了单根继承的冗余 不灵活问题。
  d.接口可以有多继承。
  e.类是先继承类，在实现接口
  f.接口和类没关系
  g.接口中的变量都是常量。
6.类的三大特性；
  封装：encapsulation
  继承：inheritance
  作业：
  多态：polymorphism
  a.虚方法调用
  b。形参多态。
  c。返回值多态。
7、作业：各个国家饮食问题，用多态做了出来。

